@model TechMove.Models.ServiceRequest

@{
    ViewData["Title"] = "Service Request Details";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h1>
        <i class="fas fa-clipboard-list"></i> Service Request Details
    </h1>
    <div>
        <a asp-action="Index" class="btn btn-secondary">
            <i class="fas fa-arrow-left"></i> Back to List
        </a>
    </div>
</div>

<div class="row">
    <div class="col-md-8">
        <div class="card mb-4">
            <div class="card-header bg-info text-white">
                <h5><i class="fas fa-info-circle"></i> Request Information</h5>
            </div>
            <div class="card-body">
                <dl class="row">
                    <dt class="col-sm-3">Request ID</dt>
                    <dd class="col-sm-9">@Model.ServiceRequestId</dd>

                    <dt class="col-sm-3">Title</dt>
                    <dd class="col-sm-9">@Model.RequestTitle</dd>

                    <dt class="col-sm-3">Description</dt>
                    <dd class="col-sm-9">@Model.Description</dd>

                    <dt class="col-sm-3">Status</dt>
                    <dd class="col-sm-9">
                        <span class="badge bg-@(Model.Status.ToString().ToLower() switch {
                            "pending" => "warning",
                            "inprogress" => "info",
                            "completed" => "success",
                            "cancelled" => "danger",
                            _ => "secondary"
                        }) p-2">
                            @Model.Status
                        </span>
                    </dd>

                    <dt class="col-sm-3">Created Date</dt>
                    <dd class="col-sm-9">@Model.CreatedAt.ToString("yyyy-MM-dd HH:mm")</dd>

                    @if (Model.UpdatedAt.HasValue)
                    {
                        <dt class="col-sm-3">Last Updated</dt>
                        <dd class="col-sm-9">@Model.UpdatedAt.Value.ToString("yyyy-MM-dd HH:mm")</dd>
                    }
                </dl>
            </div>
        </div>
    </div>

    <div class="col-md-4">
        <div class="card mb-4">
            <div class="card-header bg-success text-white">
                <h5><i class="fas fa-dollar-sign"></i> Financial Details</h5>
            </div>
            <div class="card-body">
                <dl class="row">
                    <dt class="col-sm-6">Amount (USD)</dt>
                    <dd class="col-sm-6">$@Model.AmountUSD.ToString("N2")</dd>

                    <dt class="col-sm-6">Amount (ZAR)</dt>
                    <dd class="col-sm-6">R@Model.AmountZAR.ToString("N2")</dd>

                    <dt class="col-sm-6">Exchange Rate</dt>
                    <dd class="col-sm-6">@Model.ExchangeRateUsed.ToString("N4")</dd>
                </dl>
            </div>
        </div>
    </div>
</div>

<div class="row">
    <div class="col-md-12">
        <div class="card mb-4">
            <div class="card-header bg-primary text-white">
                <h5><i class="fas fa-file-contract"></i> Contract Information</h5>
            </div>
            <div class="card-body">
                <dl class="row">
                    <dt class="col-sm-2">Contract Number</dt>
                    <dd class="col-sm-10">@Model.Contract?.ContractNumber</dd>

                    <dt class="col-sm-2">Client Name</dt>
                    <dd class="col-sm-10">@Model.Contract?.Client?.Name</dd>

                    <dt class="col-sm-2">Client Email</dt>
                    <dd class="col-sm-10">@Model.Contract?.Client?.Email</dd>

                    <dt class="col-sm-2">Service Lead</dt>
                    <dd class="col-sm-10">@Model.Contract?.ServiceLead</dd>

                    <dt class="col-sm-2">Contract Status</dt>
                    <dd class="col-sm-10">
                        <span class="badge bg-@(Model.Contract?.Status.ToString().ToLower() switch {
                            "active" => "success",
                            "expired" => "danger",
                            "draft" => "secondary",
                            "onhold" => "warning",
                            _ => "secondary"
                        })">
                            @Model.Contract?.Status
                        </span>
                    </dd>

                    <dt class="col-sm-2">Valid Period</dt>
                    <dd class="col-sm-10">
                        @Model.Contract?.StartDate.ToString("yyyy-MM-dd") to @Model.Contract?.EndDate.ToString("yyyy-MM-dd")
                    </dd>
                </dl>
            </div>
        </div>
    </div>
</div>

<!-- Status Update Section -->
<div class="row">
    <div class="col-md-12">
        <div class="card">
            <div class="card-header bg-warning">
                <h5><i class="fas fa-exchange-alt"></i> Update Status</h5>
            </div>
            <div class="card-body">
                <form asp-action="UpdateStatus" method="post" class="row g-3">
                    <input type="hidden" name="id" value="@Model.ServiceRequestId" />
                    <div class="col-md-6">
                        <select name="newStatus" class="form-select" asp-items="Html.GetEnumSelectList<TechMove.Models.RequestStatus>()">
                        </select>
                    </div>
                    <div class="col-md-6">
                        <button type="submit" class="btn btn-primary">
                            <i class="fas fa-save"></i> Update Status
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        
        $(document).ready(function() {
            var currentStatus = '@Model.Status';
            $('select[name="newStatus"]').val(currentStatus);
        });
    </script>
}
@* References
ahmed, F., 2026. Views In ASP.NET MVC. [Online]
Available at: https://www.c-sharpcorner.com/article/views-in-asp-net-mvc/
alex.mironov, 2013. How do you request static .html files under the ~/Views folder in ASP.NET MVC?. [Online]
Available at: https://stackoverflow.com/questions/17949460/how-do-you-request-static-html-files-under-the-views-folder-in-asp-net-mvc
Microsoft, 2026. Views in ASP.NET Core MVC. [Online]
Available at: https://learn.microsoft.com/en-us/aspnet/core/mvc/views/overview?view=aspnetcore-10.0
w3schools, 2026. HTML Introduction. [Online]
Available at: https://www.w3schools.com/html/html_intro.asp *@