@model IEnumerable<TechMove.Models.ServiceRequest>

@{
    ViewData["Title"] = "Service Requests";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h1>
        <i class="fas fa-clipboard-list"></i> Service Requests
    </h1>
    <a asp-action="Create" class="btn btn-primary">
        <i class="fas fa-plus"></i> New Service Request
    </a>
</div>

<!-- Search and Filter Section -->
<div class="card mb-4">
    <div class="card-body">
        <form method="get" class="row g-3">
            <div class="col-md-4">
                <label for="status" class="form-label">Filter by Status</label>
                <select name="status" id="status" class="form-select" asp-items="Html.GetEnumSelectList<TechMove.Models.RequestStatus>()">
                    <option value="">All Status</option>
                </select>
            </div>
            <div class="col-md-4">
                <label for="contractNumber" class="form-label">Contract Number</label>
                <input type="text" name="contractNumber" id="contractNumber" class="form-control" placeholder="Search by contract number..." />
            </div>
            <div class="col-md-4 d-flex align-items-end">
                <button type="submit" class="btn btn-primary me-2">
                    <i class="fas fa-search"></i> Search
                </button>
                <a asp-action="Index" class="btn btn-secondary">
                    <i class="fas fa-redo"></i> Reset
                </a>
            </div>
        </form>
    </div>
</div>

@if (!Model.Any())
{
    <div class="alert alert-info">
        <i class="fas fa-info-circle"></i> No service requests found. 
        <a asp-action="Create">Create your first service request</a>
    </div>
}
else
{
    <div class="table-responsive">
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Title</th>
                    <th>Contract #</th>
                    <th>Client</th>
                    <th>Amount (USD)</th>
                    <th>Amount (ZAR)</th>
                    <th>Status</th>
                    <th>Created</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.ServiceRequestId</td>
                        <td>@item.RequestTitle</td>
                        <td>@item.Contract?.ContractNumber</td>
                        <td>@item.Contract?.Client?.Name</td>
                        <td>$@item.AmountUSD.ToString("N2")</td>
                        <td>R@item.AmountZAR.ToString("N2")</td>
                        <td>
                            <span class="badge bg-@(item.Status.ToString().ToLower() switch {
                                "pending" => "warning",
                                "inprogress" => "info",
                                "completed" => "success",
                                "cancelled" => "danger",
                                _ => "secondary"
                            })">
                                @item.Status
                            </span>
                         </td>
                        <td>@item.CreatedAt.ToString("yyyy-MM-dd")</td>
                        <td>
                            <a asp-action="Details" asp-route-id="@item.ServiceRequestId" class="btn btn-sm btn-info">
                                <i class="fas fa-info-circle"></i>
                            </a>
                            <button type="button" class="btn btn-sm btn-warning" data-bs-toggle="modal" data-bs-target="#statusModal" 
                                    data-id="@item.ServiceRequestId" data-status="@item.Status">
                                <i class="fas fa-exchange-alt"></i>
                            </button>
                         </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
}

<!-- Summary Cards -->
<div class="row mt-4">
    <div class="col-md-3">
        <div class="card bg-primary text-white">
            <div class="card-body">
                <h6 class="card-title">Total Requests</h6>
                <h3 class="mb-0">@Model.Count()</h3>
            </div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card bg-warning text-dark">
            <div class="card-body">
                <h6 class="card-title">Pending</h6>
                <h3 class="mb-0">@Model.Count(r => r.Status == RequestStatus.Pending)</h3>
            </div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card bg-info text-white">
            <div class="card-body">
                <h6 class="card-title">In Progress</h6>
                <h3 class="mb-0">@Model.Count(r => r.Status == RequestStatus.InProgress)</h3>
            </div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card bg-success text-white">
            <div class="card-body">
                <h6 class="card-title">Completed</h6>
                <h3 class="mb-0">@Model.Count(r => r.Status == RequestStatus.Completed)</h3>
            </div>
        </div>
    </div>
</div>

<!-- Update Status Modal -->
<div class="modal fade" id="statusModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header bg-primary text-white">
                <h5 class="modal-title">
                    <i class="fas fa-exchange-alt"></i> Update Request Status
                </h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
            </div>
            <form asp-action="UpdateStatus" method="post">
                <div class="modal-body">
                    <input type="hidden" name="id" id="requestId" />
                    <div class="mb-3">
                        <label for="newStatus" class="form-label">New Status</label>
                        <select name="newStatus" id="newStatus" class="form-select" asp-items="Html.GetEnumSelectList<TechMove.Models.RequestStatus>()"></select>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                    <button type="submit" class="btn btn-primary">Update Status</button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        
        $('#statusModal').on('show.bs.modal', function (event) {
            var button = $(event.relatedTarget);
            var requestId = button.data('id');
            var currentStatus = button.data('status');
            
            var modal = $(this);
            modal.find('#requestId').val(requestId);
            modal.find('#newStatus').val(currentStatus);
        });
    </script>
}
@* References
ahmed, F., 2026. Views In ASP.NET MVC. [Online]
Available at: https://www.c-sharpcorner.com/article/views-in-asp-net-mvc/
alex.mironov, 2013. How do you request static .html files under the ~/Views folder in ASP.NET MVC?. [Online]
Available at: https://stackoverflow.com/questions/17949460/how-do-you-request-static-html-files-under-the-views-folder-in-asp-net-mvc
Microsoft, 2026. Views in ASP.NET Core MVC. [Online]
Available at: https://learn.microsoft.com/en-us/aspnet/core/mvc/views/overview?view=aspnetcore-10.0
w3schools, 2026. HTML Introduction. [Online]
Available at: https://www.w3schools.com/html/html_intro.asp *@